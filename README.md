# Nodejs based backend REST services for creating and grouping Data Model resources


## Docker build
`
docker build -t dm-service .
`


## Docker run
`
docker run --name dm-service -p 3011:3011 -d dm-service 
`
